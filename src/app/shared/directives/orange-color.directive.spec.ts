import { OrangeColorDirective } from './orange-color.directive';

describe('OrangeColorDirective', () => {
  it('should create an instance', () => {
    const directive = new OrangeColorDirective();
    expect(directive).toBeTruthy();
  });
});
