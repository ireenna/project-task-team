import { GreenColorDirective } from './green-color.directive';

describe('GreenColorDirective', () => {
  it('should create an instance', () => {
    const directive = new GreenColorDirective();
    expect(directive).toBeTruthy();
  });
});
